{
  "name": "cv",
  "private": true,
  "packageManager": "npm@10.9.0",
  "scripts": {
    "build": "nest build",
    "start": "node dist/server.mjs",

    "seed-init": "export $(cat .local.env) && tsx src/seeds/init.ts",

    "contract:emit": "export $(cat .local.env) && prisma contract emit",
    "db:init": "export $(cat .local.env)  && prisma db init",
    "db:reset": "export $(cat .local.env) && psql $DATABASE_URL -c 'DROP SCHEMA public CASCADE; CREATE SCHEMA public; DROP SCHEMA prisma_contract CASCADE;' && npm run migrate && npm run seed-init",
    "db:update": "export $(cat .local.env) && prisma db update",
    "db:verify": "export $(cat .local.env) && prisma db verify",
    "migrate": "export $(cat .local.env) && prisma db migrate --to=db",
    "migration:plan": "export $(cat .local.env) && prisma migration plan",
    "migration:status": "export $(cat .local.env) && prisma migration status",

    "skills:sync": "export $(cat .local.env) && prisma skills sync || exit 0",
    "postinstall": "export $(cat .local.env) && prisma skills sync || exit 0",
    "relay": "cp src/schema.gql front/schema.gql && npm --prefix front run relay",
    "relay:watch": "npm --prefix front run relay:watch"
  },
  "dependencies": {
    "@apollo/server": "^5.5.1",
    "@as-integrations/express5": "^1.1.2",
    "@nestjs/apollo": "^14.0.0",
    "@nestjs/common": "^12.0.1",
    "@nestjs/core": "^12.0.1",
    "@nestjs/graphql": "^14.0.0",
    "@nestjs/platform-express": "^12.0.1",
    "@prisma/client": "^7.10.0",
    "@prisma/orm-postgres": "8.0.0-rc.8",
    "dataloader": "^2.2.3",
    "express": "^5.2.1",
    "graphql": "^16.14.2",
    "graphql-http": "^1.23.0",
    "reflect-metadata": "^0.2.2",
    "ruru": "^2.0.1",
    "temporal-polyfill": "^1.0.4"
  },
  "devDependencies": {
    "@nestjs/cli": "^12.0.0",
    "@nestjs/schematics": "^12.0.0",
    "@swc/cli": "^0.8.1",
    "@types/express": "^5.0.6",
    "@types/node": "^25.6.2",
    "dotenv-cli": "^11.0.0",
    "esbuild": "^0.28.1",
    "prisma": "^8.0.0-rc.12",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "effect": "4.0.0-rc.112"
  }
}
